#!/usr/bin/env node
// Post-install: just print usage. SDK is bootstrapped lazily on first run.
console.log('\n✅ dev-emulator installed.\n');
console.log('Run `dev-emulator` to see usage.');
console.log('On first use, Android SDK tools are downloaded automatically if not found.\n');
